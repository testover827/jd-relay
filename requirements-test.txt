# 测试依赖 — Jenkins & 钉钉转发器项目
# Python 3.11+

# 核心测试框架
pytest>=7.4.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0

# HTTP 异步客户端（FastAPI ASGI 测试）
httpx>=0.25.0,<1.0.0

# HTTP Mock 框架（模拟外部 API）
respx>=0.21.0,<1.0.0

# SQLite 异步驱动（测试数据库）
aiosqlite>=0.19.0,<1.0.0

# 代码覆盖率
pytest-cov>=4.1.0,<6.0.0

# 时间 Mock（时间敏感测试）
freezegun>=1.2.0,<2.0.0

# 可选：性能分析
# pytest-profiling>=1.7.0
