# Sensitive file detection demo
# If this file exists when Jenkins build runs, the build will pause
# at an input step and trigger the second review flow.
#
# Demo: create this file in the Jenkins workspace to simulate
# a sensitive file modification.
